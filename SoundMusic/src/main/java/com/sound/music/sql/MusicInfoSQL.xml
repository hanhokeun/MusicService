<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC 
				"-//mybatis.org//DTD Mapper 3.0//EN" 
				"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="musicinfo">
	<!-- 01. 음악 상세정보조회  -->
	<select id="minfo" resultType="com.sound.music.vo.MusicInfoVO">
		SELECT 
			MU_NO as no,
			MU_TITLE as title,
			MU_ARTIST as artist,
			MU_ALBUM as album,
			MU_IMAGEPATH as path,
			MU_GENRE as genre,
			MU_LYRICS as lyrics,
			MU_STAR as star
		FROM
			MUSIC
		WHERE
			MU_ISSHOW='Y'
		AND 
			MU_NO = #{no}	
	</select>
</mapper>				